{
  "name": "preact-markdown",
  "version": "0.4.1",
  "description": "Markdown in Preact Markup",
  "main": "index.js",
  "scripts": {
    "test": "mocha"
  },
  "repository": "preact-markdown",
  "dependencies": {
    "marked": "^0.3.6",
    "preact-markup": "^1.6.0"
  },
  "peer-dependencies": {
    "preact": "*"
  },
  "devDependencies": {
    "mocha": "^5.2.0"
  }
}
