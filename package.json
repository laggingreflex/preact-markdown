{
  "name": "preact-markdown",
  "version": "0.5.0",
  "description": "Markdown in Preact Markup",
  "main": "index.js",
  "scripts": {
    "test": "mocha"
  },
  "repository": "laggingreflex/preact-markdown",
  "dependencies": {
    "marked": "^0.8.0",
    "preact-markup": "^2.0.0"
  },
  "peer-dependencies": {
    "preact": "10.x"
  },
  "devDependencies": {
    "preact": "^10.1.0",
    "mocha": "^6.2.2"
  }
}
