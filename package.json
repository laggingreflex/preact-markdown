{
  "name": "preact-markdown",
  "version": "0.5.0",
  "description": "Markdown in Preact Markup",
  "main": "index.js",
  "scripts": {
    "test": "mocha"
  },
  "repository": "preact-markdown",
  "dependencies": {
    "marked": "^0.6.2",
    "preact-markup": "^1.6.0"
  },
  "peer-dependencies": {
    "preact": "*"
  },
  "devDependencies": {
    "mocha": "^5.2.0"
  }
}
